# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Env / secrets
.env

# Local dev compose override (bind-mounts frontend for live visual iteration)
infra/docker-compose.local.yml

# Persisted runtime settings (local-dev default location; Docker uses a volume)
data/

# OS / editor
.DS_Store
*.swp

# Claude Code local session settings (auto-generated, not shared config)
.claude/

# Eval run outputs (generated by evals/run.py; regenerable, go stale — the
# corpus in cases.jsonl is the tracked artifact, not any single run)
backend/evals/*.csv
